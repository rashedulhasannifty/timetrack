'use client';

// The boundary's body lives in components/ui/SegmentError; Next requires this segment file
// itself to be a Client Component export.
import { SegmentError } from '../../components/ui/SegmentError';

export default SegmentError;
