import { PageHeader } from '../../../components/ui/PageHeader';

export default function ReportsPage() {
  return (
    <>
      <PageHeader title="Reports" subtitle="Date-range summaries and CSV export (PRD §6.5)." />
      <p className="text-sm text-neutral-500">Scaffold. Manager/Admin only, enforced by the API.</p>
    </>
  );
}
